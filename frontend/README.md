# PIP Frontend

React + Vite dashboard for PIP (Payment Incident Platform). See the [root README](../README.md) for full setup, environment variables, and deployment instructions.

## Commands

```bash
npm install
npm run dev       # http://localhost:5173
npm run build     # production build to dist/
npm run preview   # preview the production build locally
npm run lint      # ESLint
```
